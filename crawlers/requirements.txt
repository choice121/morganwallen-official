# Morgan Wallen site — content crawlers
# Python 3.11+

# Web crawling
crawl4ai>=0.4.0

# HTTP client (async)
httpx>=0.27.0

# Environment variable loading
python-dotenv>=1.0.0

# Supabase is accessed via REST API (no SDK needed — avoids version conflicts)
# YouTube and Spotify are accessed via REST APIs as well
